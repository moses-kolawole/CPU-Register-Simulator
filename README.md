# CPU-Register-Simulator
This project is a simple simulation of four important CPU registers using the C programming language. Since normal C programs cannot directly access the actual hardware registers inside a processor, I used variables to represent them and demonstrate how their values change during program execution.
